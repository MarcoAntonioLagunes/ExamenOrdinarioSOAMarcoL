# Frontend unificado

Abre `index.html` en tu navegador (mejor usando una extensión de Live Server o un servidor estático)
y asegúrate de tener levantados:

- Servicio SOAP en `http://localhost:8000/soap`
- API REST en `http://localhost:8080/api/cursos`
